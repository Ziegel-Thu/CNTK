# tests

Tests for shared code in `src/`.

Priority tests:

- spectral tail calculations on hand-constructed matrices
- kernel metric calculations
- opposite-label collision graph statistics
- kernel gradient-flow simulator on diagonal kernels
